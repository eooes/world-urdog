{
  "name": "shinsekai-game",
  "version": "1.0.0",
  "description": "SHIN SEKAI — 3D Virtual Concert client scaffold",
  "type": "module",
  "scripts": {
    "dev":    "npx --yes serve . -p 8080 --cors",
    "server": "cd server && npm install && node index.js",
    "start":  "npx --yes serve . -p 8080 --cors"
  }
}
